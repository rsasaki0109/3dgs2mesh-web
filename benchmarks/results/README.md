# Community benchmark results

No unverified or synthetic performance claims are committed here. Contributors can attach the locally downloaded benchmark JSON to a GPU compatibility issue. Maintainers may add reviewed reports to this directory with consent; the source 3DGS asset is never included.

Run `npm run benchmark:summary` to render a Markdown table from reviewed JSON files.
